# HTML Notes
## HTML Basics
- HTML stands for HyperText Markup Language.
- HTML is the standard markup language for creating Web pages.
- HTML describes the structure of a Web page.
- HTML consists of a series of elements.
- HTML elements tell the browser how to display the content.
- HTML elements label pieces of content such as "this is a heading", "this is a paragraph", "this is a link", etc.
### HTML Document
- HTML documents are text documents.
- HTML documents must have a .html or .htm extension.
- HTML documents should be saved using the UTF-8 character encoding.
- HTML documents should begin with a document type declaration:
```
<!DOCTYPE html>
```
- HTML documents should have a root element: 
```
<html>
```

```
- The HTML document should have a   <head> section and a <body> section.
- The <head> section contains meta information about the HTML document.
- The <body> section contains the visible page content.
- The <title> element specifies a title for the HTML document.
- The <h1> element defines a large heading.
- The <p> element defines a paragraph.
- The <a> element defines a hyperlink.
- The <img> element defines an image.
- The <br> element defines a line break.
- The <hr> element defines a thematic change in the content.
- The <div> element defines a division or a section in an HTML document.
- The <span> element defines a section in a document.
- The <table> element defines a table.
- The <tr> element defines a table row.
- The <th> element defines a table header.
- The <td> element defines a table cell.
- The <thead> element groups the header content in a table.
- The <tbody> element groups the body content in a table.
- The <tfoot> element groups the footer content in a table.
- The <ul> element defines an unordered list.
- The <ol> element defines an ordered list.
- The <li> element defines a list item.
- The <dl> element defines a description list.
- The <dt> element defines a term/name in a description list.
- The <dd> element defines a description of a term/name in a description list.
- The <form> element defines an HTML form for user input.
- The <input> element defines an input control
- The <button> element defines a clickable button.
- The <select> element defines a drop-down list.
- The <option> element defines an option in a drop-down list.
- The <textarea> element defines a multiline input control (text area).
- The <label> element defines a label for an <input> element.
- The <fieldset> element groups related elements in a form.
- The <legend> element defines a caption for a <fieldset> element.
- The <iframe> element defines an inline frame.
- The <meta> element provides metadata about the HTML document.
- The <style> element defines style information for a document.
- The <script> element defines a client-side script.
- The <noscript> element defines an alternate content for users that do not support client-side scripts.
- The <header> element defines a header for a document or section.
- The <nav> element defines a set of navigation links.
- The <section> element defines a section in a document.
- The <article> element defines an independent self-contained article.
- The <aside> element defines content aside from the page content.
- The <footer> element defines a footer for a document or section.
- The <details> element defines additional details that the user can view or hide.
- The <summary> element defines a visible heading for a <details> element.
- The <dialog> element defines a dialog box or window.
- The <main> element specifies the main content of a document.
- The <mark> element defines marked/highlighted text.
- The <time> element defines a date/time.
- The <progress> element represents the progress of a task.
- The <meter> element defines a scalar measurement within a known range (a gauge).
- The <figure> element specifies self-contained content, like illustrations, diagrams, photos, code listings, etc.
- The <figcaption> element defines a caption for a <figure> element.
- The <audio> element defines sound content.
- The <video> element defines a video or movie.
- The <source> element defines multiple media resources for media elements (<video> and <audio>).
- The <embed> element defines a container for an external (non-HTML) application.
- The <object> element defines an embedded object.
- The <param> element defines parameters for an <object> element.
- The <canvas> element is used to draw graphics, on the fly, via scripting (usually JavaScript).
- The <map> element defines a client-side image-map.
- The <area> element defines an area inside an image-map.
- The <svg> element defines a container for SVG graphics.
- The <math> element defines a container for mathematical expressions.
- The <head> element is a container for all the head elements.
- The <title> element specifies a title for the document.
- The <base> element specifies the base URL/target for all relative URLs in a document.
- The <link> element defines the relationship between a document and an external resource (most used to link to style sheets).
```
